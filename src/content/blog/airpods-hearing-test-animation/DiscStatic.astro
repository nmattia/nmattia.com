---
import QuadShaderMain from "./QuadShaderMain.astro";
---

<QuadShaderMain
  uniforms={[{ name: "uRadius", min: 0.1, max: 1, value: 0.66 }]}
  main={`
        return 1. - step(uRadius, length(uv));
`}
/>
